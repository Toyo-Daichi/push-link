version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /frontend
    files:
      - '**/*'  